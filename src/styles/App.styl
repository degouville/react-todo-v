#root
  max-width 1280px
  margin 0 auto
  padding 2rem

body
  margin 0
  font-family 'Geist Sans', sans-serif
  min-width 320px
  min-height 100vh
  display flex
  place-items center

  h1, h2, h2
    text-transform uppercase